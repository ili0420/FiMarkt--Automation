<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fimarkt.UserPage"
             >
    <VerticalStackLayout>
        <Button Text="LİG ARAT" BorderColor="Transparent" x:Name="btnLigSearch" Clicked="btnLigSearch_Clicked"  ></Button>

        <Button Text="TAKIM ARAT" BorderColor="Transparent" x:Name="btnTeamSearch" Clicked="btnTeamSearch_Clicked"></Button>

        <Button Text="ÜLKE ARAT" BorderColor="Transparent" x:Name="btnCountrySearch" Clicked="btnCountrySearch_Clicked"></Button>

        <Button Text="FUTBOLCU ARAT" BorderColor="Transparent" x:Name="btnBallerSearch" Clicked="btnBallerSearch_Clicked"></Button>

        <Button Text="ÇIKIŞ YAP" BorderColor="Transparent" x:Name="btnLogOut" Clicked="btnLogOut_Clicked"></Button>

        <Button Text="ŞİFRE DEĞİŞTİR" BorderColor="Transparent" x:Name="changePassword" Clicked="changePassword_Clicked"></Button>

    </VerticalStackLayout>
</ContentPage>